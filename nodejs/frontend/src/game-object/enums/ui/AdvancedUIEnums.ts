/**
 * Advanced UI Enums
 * 
 * Enums for advanced UI components and their specific functionality
 */

// ============================================================================
// ADVANCED UI ENUMS
// ============================================================================

/**
 * Progress bar orientations enum
 */
export enum ProgressBarOrientation {
  HORIZONTAL = 'horizontal',
  VERTICAL = 'vertical',
  CUSTOM = 'custom',
}

/**
 * Progress bar fill modes enum
 */
export enum ProgressBarFillMode {
  LEFT_TO_RIGHT = 'left-to-right',
  RIGHT_TO_LEFT = 'right-to-left',
  TOP_TO_BOTTOM = 'top-to-bottom',
  BOTTOM_TO_TOP = 'bottom-to-top',
  CENTER_OUT = 'center-out',
  CUSTOM = 'custom',
}

/**
 * Checkbox states enum
 */
export enum CheckboxState {
  UNCHECKED = 'unchecked',
  CHECKED = 'checked',
  INDETERMINATE = 'indeterminate',
  DISABLED = 'disabled',
  CUSTOM = 'custom',
}

/**
 * Radio button states enum
 */
export enum RadioButtonState {
  UNCHECKED = 'unchecked',
  CHECKED = 'checked',
  DISABLED = 'disabled',
  CUSTOM = 'custom',
}

/**
 * Tooltip positions enum
 */
export enum TooltipPosition {
  TOP = 'top',
  BOTTOM = 'bottom',
  LEFT = 'left',
  RIGHT = 'right',
  TOP_LEFT = 'top-left',
  TOP_RIGHT = 'top-right',
  BOTTOM_LEFT = 'bottom-left',
  BOTTOM_RIGHT = 'bottom-right',
  CUSTOM = 'custom',
}

/**
 * Tooltip triggers enum
 */
export enum TooltipTrigger {
  HOVER = 'hover',
  CLICK = 'click',
  FOCUS = 'focus',
  MANUAL = 'manual',
  CUSTOM = 'custom',
}

/**
 * Tab positions enum
 */
export enum TabPosition {
  TOP = 'top',
  BOTTOM = 'bottom',
  LEFT = 'left',
  RIGHT = 'right',
  CUSTOM = 'custom',
}

/**
 * Tab alignments enum
 */
export enum TabAlignment {
  START = 'start',
  CENTER = 'center',
  END = 'end',
  STRETCH = 'stretch',
  CUSTOM = 'custom',
}

/**
 * Scroll view orientations enum
 */
export enum ScrollViewOrientation {
  HORIZONTAL = 'horizontal',
  VERTICAL = 'vertical',
  BOTH = 'both',
  CUSTOM = 'custom',
}

/**
 * Dropdown states enum
 */
export enum DropdownState {
  CLOSED = 'closed',
  OPEN = 'open',
  DISABLED = 'disabled',
  CUSTOM = 'custom',
}

/**
 * Slider orientations enum
 */
export enum SliderOrientation {
  HORIZONTAL = 'horizontal',
  VERTICAL = 'vertical',
  CUSTOM = 'custom',
}

/**
 * Slider value change modes enum
 */
export enum SliderValueChangeMode {
  CONTINUOUS = 'continuous',
  DISCRETE = 'discrete',
  CUSTOM = 'custom',
}

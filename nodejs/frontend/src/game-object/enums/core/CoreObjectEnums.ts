/**
 * Core Object Enums
 *
 * Enums for core game objects and their specific functionality
 */

// ============================================================================
// CORE OBJECT ENUMS
// ============================================================================

/**
 * Dynamic bitmap text alignment enum
 */
export enum DynamicBitmapTextAlign {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
  CUSTOM = 'custom',
}

/**
 * Dynamic bitmap text blend modes enum
 */
export enum DynamicBitmapTextBlendMode {
  NORMAL = 'normal',
  ADD = 'add',
  MULTIPLY = 'multiply',
  SCREEN = 'screen',
  OVERLAY = 'overlay',
  CUSTOM = 'custom',
}

/**
 * Dynamic bitmap text word wrap modes enum
 */
export enum DynamicBitmapTextWordWrap {
  NONE = 'none',
  WORD = 'word',
  CHARACTER = 'character',
  CUSTOM = 'custom',
}

/**
 * Dynamic bitmap text animation modes enum
 */
export enum DynamicBitmapTextAnimationMode {
  NONE = 'none',
  TYPEWRITER = 'typewriter',
  FADE_IN = 'fade-in',
  SLIDE_IN = 'slide-in',
  CUSTOM = 'custom',
}

/**
 * Tile sprite tile modes enum
 */
export enum TileSpriteTileMode {
  REPEAT = 'repeat',
  CLAMP = 'clamp',
  MIRROR = 'mirror',
  CUSTOM = 'custom',
}

/**
 * Tile sprite blend modes enum
 */
export enum TileSpriteBlendMode {
  NORMAL = 'normal',
  ADD = 'add',
  MULTIPLY = 'multiply',
  SCREEN = 'screen',
  OVERLAY = 'overlay',
  CUSTOM = 'custom',
}

/**
 * Bitmap text alignment enum
 */
export enum BitmapTextAlign {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
  CUSTOM = 'custom',
}

/**
 * Bitmap text blend modes enum
 */
export enum BitmapTextBlendMode {
  NORMAL = 'normal',
  ADD = 'add',
  MULTIPLY = 'multiply',
  SCREEN = 'screen',
  OVERLAY = 'overlay',
  CUSTOM = 'custom',
}

/**
 * Bitmap text word wrap modes enum
 */
export enum BitmapTextWordWrap {
  NONE = 'none',
  WORD = 'word',
  CHARACTER = 'character',
  CUSTOM = 'custom',
}

/**
 * Graphics line styles enum
 */
export enum GraphicsLineStyle {
  SOLID = 'solid',
  DASHED = 'dashed',
  DOTTED = 'dotted',
  CUSTOM = 'custom',
}

/**
 * Graphics fill styles enum
 */
export enum GraphicsFillStyle {
  SOLID = 'solid',
  GRADIENT = 'gradient',
  PATTERN = 'pattern',
  CUSTOM = 'custom',
}

/**
 * Graphics path commands enum
 */
export enum GraphicsPathCommand {
  MOVE_TO = 'moveTo',
  LINE_TO = 'lineTo',
  CURVE_TO = 'curveTo',
  ARC = 'arc',
  CUSTOM = 'custom',
}

/**
 * Sprite blend modes enum
 */
export enum SpriteBlendMode {
  NORMAL = 'normal',
  ADD = 'add',
  MULTIPLY = 'multiply',
  SCREEN = 'screen',
  OVERLAY = 'overlay',
  CUSTOM = 'custom',
}

/**
 * Sprite crop modes enum
 */
export enum SpriteCropMode {
  NONE = 'none',
  MANUAL = 'manual',
  AUTOMATIC = 'automatic',
  CUSTOM = 'custom',
}

/**
 * UnitType Enum
 * Defines the types of units in the system
 */
export enum UnitType {
  SIZE = 'size', // Size units (width, height)
  POSITION = 'position', // Position units (x, y)
  SCALE = 'scale', // Scale units (scaleX, scaleY)
}

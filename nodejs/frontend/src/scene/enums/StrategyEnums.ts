/**
 * Strategy System Enums
 *
 * Enums for scene building and validation strategies.
 */

// ============================================================================
// STRATEGY ENUMS
// ============================================================================

/**
 * Scene building strategy operations enum
 */
export enum SceneBuildingStrategyOperation {
  CREATE_SCENE = 'create_scene',
  BUILD_ELEMENTS = 'build_elements',
  CONFIGURE_SCENE = 'configure_scene',
  VALIDATE_SCENE = 'validate_scene',
  OPTIMIZE_SCENE = 'optimize_scene',
  DESTROY_SCENE = 'destroy_scene',
}
